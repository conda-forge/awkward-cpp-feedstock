{% set name = "awkward-cpp" %}
{% set version = "16" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/awkward-cpp-{{ version }}.tar.gz
  sha256: c84d5d5856b0e242fa837f0db608087d958956179bd55377ea393f139c1b9b86

build:
  script:
    - {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - {{ compiler('cxx') }}
    - cmake >=3.15
    - make
  host:
    - python
    - pip
    - scikit-build-core >=0.1.3
    - pybind11
  run:
    - python
    - importlib-resources  # [py<39]
    - numpy >=1.17.0

test:
  imports:
    - awkward_cpp
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/awkward-cpp/
  summary: CPU kernels and compiled extensions for Awkward Array
  license: BSD-3-Clause
  license_file:
    - LICENSE
    - rapidjson/license.txt
    - rapidjson/bin/jsonschema/LICENSE

extra:
  recipe-maintainers:
    - henryiii
    - conda-forge/awkward
    - conda-forge/scikit-hep
